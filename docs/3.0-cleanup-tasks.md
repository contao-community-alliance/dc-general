# dc-general: Legacy-/Removal-Aufgabenliste (Richtung 3.0)

> Bestandsaufnahme der Deprecation-/Legacy-Marker in `src/` (Stand: Contao-5.7-Umbau,
> Branch `release/2.5.0`). Zweck: geordnete Grundlage für die 3.0-Bereinigung.
>
> **Wichtig:** Alles unten ist dc-generals **eigene** API. Entfernen = BC-Break →
> gehört an ein **Major (3.0)**, nicht an 2.5. (Die reinen Contao-<5.7-Leichen ohne
> API-Charakter — `TreeSelect`, `FileSelect` — wurden bereits in 2.5 entfernt.)

## 0. Sonderfall FileTree (Contao-Picker) — höchste Priorität

`src/Contao/View/Contao2BackendView/Widget/FileTree.php`

- `@deprecated` seit **Contao 4.13** (der genutzte `FilePicker` wurde deprecatet); nutzt
  laut `FileTree.php:598` einen **in Contao 5 entfernten** File-Selector „dynamisch".
- **Aber weiterhin aktiv verdrahtet** — nicht einfach löschbar:
  - `Resources/contao/config/config.php:27` (`use … Widget\FileTree`) + `:49`
    (`[FileTree::class, 'updateAjax']` als AJAX-Handler registriert)
  - `Contao/View/Contao2BackendView/Subscriber/WidgetBuilder.php:104`
    (`'fileTree' => FileTree::class` — Widget für Feldtyp `fileTree`)

**Aufgabe:** `fileTree`-Widget auf den aktuellen Contao-Picker/`Contao\Picker`-Flow
umstellen (Ersatz schaffen), dann `FileTree` + AJAX-Handler-Registrierung entfernen.
Braucht Backend-Test mit einem `fileTree`-Feld.

## 1. Explizit „remove in 3.0" markiert (harte Kandidaten)

| Datei:Zeile | Element | Marker |
|-------------|---------|--------|
| `Controller/Ajax.php:305` | Methode | „since 2.1, remove in 3.0 — use own response exit" |
| `Contao/View/Contao2BackendView/EditMask.php:731` | `getHeadline()` | „since 2.3, removed in 3.0" (→ `getSubHeadline()`) |
| `Contao/View/Contao2BackendView/GlobalButtonRenderer.php:175,182` | Code-Zweige | „Remove in 3.0" |
| `Contao/View/Contao2BackendView/ActionHandler/MultipleHandler/OverrideAllHandler.php:164` | Methode | „since 2.1, remove in 3.0" |
| `Data/DefaultConfig.php:47` | Element | „since 2.1, removed in 3.0" |
| `Data/ConfigInterface.php:62,73` | Interface-Methoden | „since 2.1, removed in 3.0" |
| `Data/DefaultDataProviderSqlUtils.php:29` (+45,72,91) | **ganze Klasse** | „since 2.1, remove in 3.0 — DBAL nutzen" (→ `DefaultDataProviderDBalUtils`) |
| `Data/DefaultDataProvider.php:1056` | Methode | „since 2.1, removed in 3.0 — altes DB-API" |

## 2. „Use X instead" — Ersetzt, Aufrufer migrieren + entfernen

**Controller-Gott-Objekt → `ModelCollector` / `RelationshipManager`:**
`Controller/ControllerInterface.php` (73,86,104,150,254,267,281,298) und
`Controller/DefaultController.php` (204,231,256,282,483,1310,1323,1338,1353):
`searchParentOfIn`, `searchParentOf`, `collectChildrenOf`, `collectSiblingsOf`,
`getModel`, `isRoot`, `setRoot`, `setParent`, `setSameParent`.

**Model-Relationship-Listener statt View-Methoden:**
`View/ViewInterface.php:132`, `Contao/View/Contao2BackendView/TreeView.php:690`,
`Contao/View/Contao2BackendView/ParentView.php:43`
(→ `EnforceModelRelationshipEvent` / `TreeEnforcingListener` / `ParentEnforcingListener`).

**Weitere „use instead":**
| Datei:Zeile | Ersatz |
|-------------|--------|
| `Contao/View/Contao2BackendView/IdSerializer.php:32` (ganze Klasse) | `Data\ModelId` |
| `Data/DCGE.php:52,59` | `ModelInterface::SHOW_CHILDREN` / `CHILD_COLLECTIONS` |
| `Data/DefaultConfig.php:157,180` | Filter statt Methode |
| `Data/DataProviderInterface.php:220` | Reset manuell/Filter |
| `Contao/View/Contao2BackendView/TreePicker.php:315` | Update-Route |
| `Cache/Factory/DcGeneralFactoryCache.php:30` | Cache-Interface direkt |

## 3. „Should be removed / non-functional" — Aufräumen

| Datei:Zeile | Marker |
|-------------|--------|
| `Contao/Dca/Populator/HardCodedPopulator.php:41` | „Should get removed from the code base!!!!" |
| `DataDefinition/Builder/AbstractEventDrivenDataDefinitionBuilder.php:44` | „Should not be used at all." |
| `View/ActionHandler/AbstractHandler.php:38`, `AbstractEnvironmentAwareHandler.php:33` | „event listener mit wechselndem State, wird entfernt" |
| `Contao/View/Contao2BackendView/ContaoBackendViewTemplate.php:124` | „Do not use" |
| `View/ViewTemplateInterface.php:77` | „Non functional." |
| `Data/DefaultDataProvider.php:282` | „never intended to be used externally" |
| `DataDefinition/Definition/View/ListingConfigInterface.php:36` | (leer) |

## 4. NICHT entfernbar — Interface-Pflicht-Stubs (nur zur Klarstellung)

`DC/General.php:328–461` — „Only here as requirement of \editable/\listable/\DataContainer"
sowie `:202` (magic access). Diese Methoden erfüllen die **Contao-`DataContainer`-
Interface-Pflicht**; sie können nicht entfernt werden, solange `DC_General` das Interface
implementiert. Auch `Controller/Ajax.php:130,146,162` (bare `@deprecated`) vor Entfernung
auf Interface-/Signatur-Pflichten prüfen.

## Vorgeschlagene Reihenfolge für 3.0

1. **FileTree** (Abschnitt 0) — funktionaler Ersatz nötig, eigener PR + Backend-Test.
2. **Abschnitt 1** (explizite 3.0-Marker) — mechanisch, Aufrufer im Repo/Umfeld prüfen.
3. **Abschnitt 2** — pro Deprecation Aufrufer auf den Ersatz migrieren, dann Methode weg.
4. **Abschnitt 3** — Klassen/Methoden nach Referenz-Check entfernen.
5. **Abschnitt 4** — nur dokumentieren/prüfen, i. d. R. Bestand.

Jeder Removal-Schritt: Referenz-Check im Repo **und** in Konsumenten (MetaModels u. a.),
danach `psalm-baseline.xml` bereinigen (verwaiste `UnusedBaselineEntry` vermeiden) und
`phpcq` grün halten.
